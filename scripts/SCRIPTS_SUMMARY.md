# Scripts Summary

### Fix Whitespace

Removes trailing whitespace and ensures files end with newline

```bash
./scripts/fix_whitespace.sh
```

`fix_whitespace.sh` script modifies:
- All `.c` and `.h` files in project directories
- Makefiles
- Shell scripts
